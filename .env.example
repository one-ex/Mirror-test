# Environment Configuration
PIXELDRAIN_API_KEY=your_api_key_here
DEBUG=False
PORT=8080

# Optional: Set default chunk size in MB (default: 10MB - same as mirror-test.py)
CHUNK_SIZE_MB=10

# Optional: Set timeout in seconds (default: 300)
UPLOAD_TIMEOUT=300